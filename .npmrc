# Do not execute dependency lifecycle scripts during install.
# Project scripts such as `npm run build` and `npm test` remain available.
ignore-scripts=true

# Keep installs reproducible and retain security checks.
package-lock=true
audit=true
fund=false
save-exact=true
